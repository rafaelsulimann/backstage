export interface Config {
  sulimann: {
     /**
      * The auth token of the DRP instance.
      * @visibility frontend
      */
     githubToken: string;
     /**
      * The auth token of the DRP instance.
      * @visibility frontend
      */
     test: string;
  };
}
